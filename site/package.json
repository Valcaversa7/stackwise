{
  "name": "stackwise",
  "version": "1.0.0",
  "private": true,
  "description": "Stackwise — an editorial tech & productivity publication. Eleventy 11ty static site.",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --port 8080",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build && npm test",
    "test": "node scripts/verify.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "html-minifier-terser": "^5.0.0",
    "markdown-it": "^14.1.0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
